<Page x:Class="chamcong.WpfAdmin.Views.IssuesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Xử lý Khiếu nại">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Danh Sách Khiếu Nại (Chấm Công / Sản Lượng)" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

        <DataGrid x:Name="dgIssues" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column" Background="White" RowHeight="40" AlternatingRowBackground="#F9F9F9">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Nhân Viên" Binding="{Binding EmployeeName}" Width="150"/>
                <DataGridTextColumn Header="Tiêu Đề Khiếu Nại" Binding="{Binding Title}" Width="250"/>
                <DataGridTextColumn Header="Nội Dung Chi Tiết" Binding="{Binding Description}" Width="350"/>
                <DataGridTextColumn Header="Ngày Gửi" Binding="{Binding CreatedAt, StringFormat=dd/MM/yyyy HH:mm}" Width="150"/>
                
                <DataGridTemplateColumn Header="Hành Động" Width="150">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Xác nhận Đã xử lý" Background="#27AE60" Foreground="White" BorderThickness="0" Padding="10,5" Click="btnResolve_Click" CommandParameter="{Binding Id}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
